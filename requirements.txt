# eval-platform runtime dependencies.
# PostgreSQL is accessed directly through psycopg 3. There is no ORM,
# no SQLAlchemy, no Alembic, no broker queue, and no cloud object storage.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
httpx>=0.28.0
structlog>=24.4.0
prometheus-client>=0.21.0
slowapi>=0.1.9
tenacity>=9.0.0
rapidfuzz>=3.10.0
